import AsyncStorage from "@react-native-async-storage/async-storage";
import { createContext,type ReactNode,useContext,useEffect,useMemo,useState } from "react";
import { appendStoredExchange,selectConversation,type StoredChatMessage } from "@/features/persistence";
export type PersistedChatMessage=StoredChatMessage;
interface ChatHistoryValue{messages:PersistedChatMessage[];getConversation:(characterId:string,language:"en"|"zh")=>PersistedChatMessage[];appendExchange:(characterId:string,language:"en"|"zh",userText:string,characterText:string)=>void;clearConversation:(characterId:string)=>void;}
const STORAGE_KEY="ip-alive:chat-history:v1";const Context=createContext<ChatHistoryValue|undefined>(undefined);
export function ChatHistoryProvider({children}:{children:ReactNode}){const[messages,setMessages]=useState<PersistedChatMessage[]>([]);const[hydrated,setHydrated]=useState(false);useEffect(()=>{void AsyncStorage.getItem(STORAGE_KEY).then((raw)=>{if(raw){const parsed=JSON.parse(raw);setMessages(Array.isArray(parsed)?parsed:parsed.messages??[])}}).finally(()=>setHydrated(true))},[]);useEffect(()=>{if(hydrated)void AsyncStorage.setItem(STORAGE_KEY,JSON.stringify({schemaVersion:1,messages}))},[messages,hydrated]);const value=useMemo<ChatHistoryValue>(()=>({messages,getConversation:(characterId,language)=>selectConversation(messages,characterId,language),appendExchange:(characterId,language,userText,characterText)=>setMessages((current)=>appendStoredExchange(current,characterId,language,userText,characterText)),clearConversation:(characterId)=>setMessages((current)=>current.filter((message)=>message.characterId!==characterId))}),[messages]);return <Context.Provider value={value}>{children}</Context.Provider>}
export function useChatHistory(){const value=useContext(Context);if(!value)throw new Error("useChatHistory must be used inside ChatHistoryProvider");return value}
