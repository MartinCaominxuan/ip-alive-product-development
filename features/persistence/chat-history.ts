export interface StoredChatMessage{id:string;characterId:string;sender:"user"|"character";text:string;language:"en"|"zh";createdAt:string;}
export interface ConversationSummary { characterId: string; language: "en" | "zh"; messageCount: number; exchangeCount: number; firstAt?: string; lastAt?: string; latestUserText?: string; latestCharacterText?: string; }
export function appendStoredExchange(current:StoredChatMessage[],characterId:string,language:"en"|"zh",userText:string,characterText:string,now=Date.now(),limit=800){const createdAt=new Date(now).toISOString();const exchange:StoredChatMessage[]=[{id:`user-${characterId}-${now}`,characterId,sender:"user",text:userText,language,createdAt},{id:`character-${characterId}-${now+1}`,characterId,sender:"character",text:characterText,language,createdAt:new Date(now+1).toISOString()}];return[...current,...exchange].slice(-limit)}
export function selectConversation(messages:StoredChatMessage[],characterId:string,language:"en"|"zh"){return messages.filter((message)=>message.characterId===characterId&&message.language===language).sort((a,b)=>a.createdAt.localeCompare(b.createdAt))}
export function summarizeConversation(messages:StoredChatMessage[],characterId:string,language:"en"|"zh"):ConversationSummary{const selected=selectConversation(messages,characterId,language);const users=selected.filter((message)=>message.sender==="user");const character=selected.filter((message)=>message.sender==="character");return{characterId,language,messageCount:selected.length,exchangeCount:Math.min(users.length,character.length),firstAt:selected[0]?.createdAt,lastAt:selected.at(-1)?.createdAt,latestUserText:users.at(-1)?.text,latestCharacterText:character.at(-1)?.text}}
export function removeConversation(messages:StoredChatMessage[],characterId:string,language?:"en"|"zh"){return messages.filter((message)=>message.characterId!==characterId||(language!==undefined&&message.language!==language))}
