export interface StoredChatMessage{id:string;characterId:string;sender:"user"|"character";text:string;language:"en"|"zh";createdAt:string;}
export function appendStoredExchange(current:StoredChatMessage[],characterId:string,language:"en"|"zh",userText:string,characterText:string,now=Date.now(),limit=800){const createdAt=new Date(now).toISOString();const exchange:StoredChatMessage[]=[{id:`user-${characterId}-${now}`,characterId,sender:"user",text:userText,language,createdAt},{id:`character-${characterId}-${now+1}`,characterId,sender:"character",text:characterText,language,createdAt:new Date(now+1).toISOString()}];return[...current,...exchange].slice(-limit)}
export function selectConversation(messages:StoredChatMessage[],characterId:string,language:"en"|"zh"){return messages.filter((message)=>message.characterId===characterId&&message.language===language).sort((a,b)=>a.createdAt.localeCompare(b.createdAt))}
